import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ionic.photomemories",
  appName: "Photo-Memories",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
